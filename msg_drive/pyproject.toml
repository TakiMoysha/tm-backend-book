[project]
name = "msg-drive"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apos>=0.2.1.2",
]

[dependency-groups]
test = [
    "pytest>=8.3.5",
]

[tools.uv.scripts]
main = "main.py"
